# Marketing site

`index.html` is a single, self-contained, theme-aware landing page (no build, no
external assets). Host it anywhere static: GitHub Pages, Netlify, Vercel, S3/
CloudFront, or serve it from the Hub behind a reverse proxy.

Update the copy/pricing here to match `docs/PRICING.md`.
